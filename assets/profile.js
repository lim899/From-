/* ============================================================================
 * 个人主页的全部内容都在这个文件里，改这里就够了，不用碰 site.js / site.css。
 *
 * 页面会先用下面的内容立刻渲染出来，然后在后台去 GitHub 拉最新资料
 * （头像、name、bio、仓库列表）覆盖上来。所以：
 *
 *   · 你在 GitHub profile 里填了 name / bio → 主页自动同步，这里可以不管
 *   · 你新建了公开仓库           → 项目卡片自动多一张
 *   · 断网、或 GitHub 接口限流    → 自动回退到下面的 fallback，页面不会变空
 * ========================================================================= */

var SITE = {

  /* ---- 必填两处 ------------------------------------------------------- */

  // GitHub profile 里的 name 是空的，先写在这儿。等你去 GitHub 设置里填了
  // 真名，这里就会被自动覆盖（也可以一直用这里的值，它只是个默认值）。
  name: '你的名字',

  // 同上，GitHub profile 的 bio 是空的。一句话介绍你自己，会以斜体显示在姓名下方。
  tagline: '写一句话介绍你自己 —— 你做什么，在意什么。',

  /* ---- 选填 ----------------------------------------------------------- */

  githubUser: 'lim899',

  role: '',          // 身份／职业，如「独立开发者」。留空则不显示这一行
  location: '',      // 所在地，如「上海」。留空则不显示
  email: '',         // 留空则页脚和元信息里不出现邮箱

  // 刊头左上角那行小字，杂志的期号。想写别的（如「个人主页」）也行
  issue: 'ISSUE 01',

  // 页脚 + 首屏的链接。想加微博 / 小红书 / X / 博客，照格式往后加就行
  links: [
    { label: 'GitHub', url: 'https://github.com/lim899' }
  ],

  // 不想出现在项目列表里的仓库名，写进来即可（大小写不敏感）
  hideRepos: [],

  // 某个仓库想链到别处而不是 GitHub 仓库页，在这里指定。
  // From- 链到站内的金句 App —— 点进去就是能用的东西，比跳去仓库页有说服力。
  linkOverrides: {
    'From-': './quotes/'
  },

  // 最多显示几张项目卡片
  maxRepos: 6,

  /* ---- GitHub 数据快照（兜底用，一般不用手动维护）--------------------- */
  // 断网、接口限流（未登录时每小时 60 次）或请求失败时用这份数据渲染。
  fallback: {
    avatar: 'https://avatars.githubusercontent.com/u/183990374?v=4',
    repos: [
      {
        name: 'From-',
        desc: '金句收藏夹：读到好句子，手机上一键存下来的离线 PWA',
        lang: 'JavaScript',
        stars: 0,
        updated: '2026-08-23'
      }
    ]
  }
};
